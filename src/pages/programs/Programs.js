import { Container, Title } from '@mantine/core';

const Programs = () => {
  return (
    <Container
      fluid
      style={{
        padding: 0
      }}
    >
      <Title order={2}>Programas</Title>
    </Container>
  );
};

export default Programs;