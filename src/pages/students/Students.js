import { Container, Title } from '@mantine/core';

const Students = () => {
  return (
    <Container
      fluid
      style={{
        padding: 0
      }}
    >
      <Title order={2}>Alumnos</Title>
    </Container>
  );
};

export default Students;