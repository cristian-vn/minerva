import { Container, Title } from '@mantine/core';

const Teachers = () => {
  return (
    <Container
      fluid
      style={{
        padding: 0
      }}
    >
      <Title order={2}>Docentes</Title>
    </Container>
  );
};

export default Teachers;