import { Container, Title } from '@mantine/core';

const Schools = () => {
  return (
    <Container
      fluid
      style={{
        padding: 0
      }}
    >
      <Title order={2}>Empleados</Title>
    </Container>
  );
};

export default Schools;