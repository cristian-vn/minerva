import { Container, Title } from '@mantine/core';

const Schools = () => {
  return (
    <Container
      fluid
      style={{
        padding: 0
      }}
    >
      <Title order={2}>Escuelas</Title>
    </Container>
  );
};

export default Schools;