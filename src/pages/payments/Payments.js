import { Container, Title } from '@mantine/core';

const Payments = () => {
  return (
    <Container
      fluid
      style={{
        padding: 0
      }}
    >
      <Title order={2}>Pagos</Title>
    </Container>
  );
};

export default Payments;